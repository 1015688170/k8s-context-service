apiVersion: v1
kind: Service
metadata:
  name: k8s-context-service
  namespace: ai-alert
  labels:
    app: k8s-context-service
spec:
  selector:
    app: k8s-context-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
